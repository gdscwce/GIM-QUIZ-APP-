import 'package:flutter/material.dart';
class CollegeMap extends StatelessWidget {
  const CollegeMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
